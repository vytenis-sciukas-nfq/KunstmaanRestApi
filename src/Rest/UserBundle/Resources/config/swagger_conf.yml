nelmio_api_doc:
  documentation:
    definitions:
      UserList:
        properties:
          users:
            type: "array"
            users:
              allOf:
                - $ref: '#/definitions/User'
        title: "UserList"
        required: ["users"]
        type: "object"

      User:
        title: "User"
        type: "object"
        properties:
          UserName:
            type: "string"
